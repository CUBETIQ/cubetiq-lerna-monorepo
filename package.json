{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.5.1"
  },
  "scripts": {
    "bootstrap": "npx -y lerna bootstrap",
    "dev": "lerna run dev --parallel",
    "build": "lerna run build",
    "test": "lerna run test",
    "clean": "lerna clean --yes"
  }
}