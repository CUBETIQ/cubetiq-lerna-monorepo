{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.5.1"
  },
  "scripts": {
    "bootstrap": "npx -y lerna bootstrap",
    "dev": "lerna run dev --parallel",
    "build": "lerna run build",
    "test": "lerna run test"
  }
}